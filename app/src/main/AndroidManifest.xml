<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cometbites">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".app.BrowseFoodJointActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".app.ListItemsActivity" android:label="@string/app_name">

        </activity>
        <activity android:name=".app.LoginActivity" android:label="@string/login"></activity>
        <activity android:name=".app.SignupActivity" android:label="@string/signup"></activity>
        <activity android:name=".app.SignupAuthActivity" android:label="@string/signup"></activity>
        <activity android:name=".app.AddPaymentActivity" android:label="@string/signup"></activity>
    </application>

</manifest>